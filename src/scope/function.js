function greeting(){
    let userName = 'keila';
   console.log(userName);

    if (userName === 'keila') {
        console.log(`Hello ${userName} !`);
    }else{
        console.log('error');
    }
}
greeting();

console.log(userName);

// Funcion scope